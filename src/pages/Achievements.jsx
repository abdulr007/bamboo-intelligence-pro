import {motion} from "framer-motion";import useApp from "../hooks/useApp";import Card from "../components/ui/Card";export default function Achievements(){const d=useApp();return <motion.div initial={{opacity:0}} animate={{opacity:1}} className="max-w-[1300px] mx-auto"><h2 className="text-4xl font-extrabold">Achievements</h2><Card className="p-6 bg-ink text-white mt-8"><h3 className="text-3xl">{d.rank}</h3><b className="text-lime">{d.xp} XP</b></Card><div data-tour="achievement-grid" className="grid md:grid-cols-3 gap-4 mt-4">{d.missions.map(m=><Card className={`p-5 ${d.complete[m.id]?'bg-lime stamp':'opacity-50'}`}><b>{m.label}</b><p>+{m.xp} XP · {d.complete[m.id]?'Unlocked':'Locked'}</p></Card>)}</div></motion.div>}
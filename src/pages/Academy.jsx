import {useState} from "react";import {motion,AnimatePresence} from "framer-motion";import useApp from "../hooks/useApp";export default function Academy(){const d=useApp(),[open,setOpen]=useState(null);return <motion.div initial={{opacity:0}} animate={{opacity:1}} className="max-w-5xl mx-auto"><h2 className="text-4xl font-extrabold">Field Academy</h2><div data-tour="academy-lessons" className="mt-8 border-t border-ink">{d.lessons.map((x,i)=><div className="border-b border-ink"><button onClick={()=>{setOpen(open===i?null:i);d.readLesson(x.id)}} className="w-full p-5 text-left"><b>{d.readLessons.includes(x.id)?'✓ ':''}{x.title}</b><small className="block">{x.tag}</small></button><AnimatePresence>{open===i&&<motion.div initial={{height:0}} animate={{height:'auto'}} exit={{height:0}} className="overflow-hidden"><div className="grid md:grid-cols-2 gap-4 p-5"><p className="p-5 bg-[#f2eee3] border">{x.body}</p><p className="p-5 bg-stone-300 border-l-4 border-bamboo">{x.analogy}</p></div></motion.div>}</AnimatePresence></div>)}</div></motion.div>}